 My favorite technique for handling errors is to let it crash and burn as soon as possible. Surprisingly, this approach actually leads to more robust code. Here's a script that I use my backend to automatically generate an invoice in my accounting system when somebody buys a course for me and pays using Stripe. There's a couple of different functions here. One to compute a gross profit, that's just a helper function. There is an initializing function that just initializes the Stripe API using the Stripe API from an environment available. Then I have a function called get application feed that for payment, computes how much I actually had to pay Stripe in order to process the payment. And then I have another function that's called construct invoice data from Stripe. So this takes the relevant information from a Stripe payment and turns it into something that I can use to construct an invoice. So this is not a complete script, not the complete automation in it is here, but these are just a few of the components that I want to show you. Now, if you look at this particular function construct invoice data from Stripe, you can see that there are various places where errors could occur. For example, there might not be a customer that's associated with the payment intent. So this thing actually doesn't really give a customer ID or get application feed also can raise all sorts of errors, right? Or whenever I try to access, let's say, the customer metadata and then the MB contact ID that stands for Monubird contact ID, Monubird is my accounting system. That might also not work. So there's plenty of situations here where things might go wrong and we could get an error. The idea of letting it burn is that you allow your program to fail fast and visibly rather than trying to avoid every possible error scenario. This is also called the fail fast principle. Now, why is that a good thing? The first reason is simplicity and clarity. Your code becomes cleaner and more readable. Why not cluttering it with lots of error-handling blocks? The primary logic of your function remains clear. For example, let's say you want to add a bunch of error handling code to this function here. So you could, for example, add a try except block to handle the application fee. So I'm going to put this line of code in a try except block like so. Then we can basically print the error and, for example, erase it. Another problem that we then should also handle is that customer metadata, Monubird contact ID might not exist so we might get a key error. So that also puts the return statement here in a try except block. So I'm going to catch a key error and then also print that I fail to construct the invoice data. And then, maybe there are other types of errors that occur that are not key errors or value errors. So perhaps we should even add a generic error handling case, you know, just in case. So I'm going to put all of this in yet another error handling block where we just catch all exceptions just to be sure. And then let's also print that error. So now I basically handled all the errors. But at the same time, this code is now really hard to read, right? Because of all these try except statements, we get lots of indentation. It's really hard to understand now what is going on. The second problem here is that we can't really do anything with these errors. If I get some kind of error, if I want to compute the application fee, yeah, then I probably don't want to generate an invoice in any case or I have to stop, right? If there is no contact in my accounting system that's associated with the stripe customer, I also can generate an invoice. In other words, it doesn't make a whole lot of sense to add all these try except blocks here and try to handle all these errors. And by the way, error handling is just one aspect of software design. If you want to learn more about how to design a piece of software from scratch, I have a free guide for you. You can get it at iron.cold slash design guide. Contains these seven steps that I take when I design new software and hopefully it helps you avoid some of the mistakes that I made in the past. Iron.cold slash design guide, the link is also in the description of this video. The second reason to not try and handle all the errors is early detection. So let me first remove all of these try except blocks because they start to annoy me a little bit. So this I don't need and this I also don't need. And then I'm also going to remove all of this code and the end that this part. So now back where we started, when you let errors immediately crash your program, you can identify and fix bugs sooner. For example, let's take a look at get application fee. This uses a so called guard clause to check where there's a charge. And the same thing here, if there's no balance transaction, it also raises a value error. The details of how this works, by the way, is not really that important. That's more of an internal stripe thing. But what matters is that we stop the function early if we don't have the information that we need. That's one of the functions of a guard clause that lets us stop and crash the program immediately with an error. And we also provide some information about why that happened. So we can potentially fix it later on. A second way that you can use a guard clause is to improve your error reporting. In that case, your application will still crash, but at least you will get a more useful error. For example, here in construct invoice data from stripe, we're going to get a key error if there is no contact ID, no moneybird contact ID. And that's maybe not going to be a very helpful error message. Instead, what you can do is add a guard clause to check if the customer has a moneybird contact ID. And in that case, we can raise a value error that specifically states that. So if there is no moneybird contact ID in the customer data, we now raise a value error. So failing fast helps you detect error sooner, and you can use guard clauses to guide that behavior. The third reason why it's useful is that serious errors are then not going to be hidden from you. If you take a look again at get application fee, you see that it can raise various errors. If charge is not there, if a balanced transaction is not there. You could also think like, hey, maybe you know, I want to make sure that the program keeps working. So if there is no charge, for example, I'm simply going to return zero. So that we get an application fee of zero. And then here, if there is no balanced transaction, well, that's also return zero in that case. So now we sort of avoid errors. However, this is really problematic. If transactions are missing from stripe or there is no charge, well, there's probably a serious issue. I definitely wouldn't want the program to assume things for me and hide those errors for me, potentially making things even worse. Because maybe there was actually an application fee, but it was reported wrongly. And then now my accounting system is messed up. Therefore, I prefer that the code just crashes and logs the error. So I know something's going on. That's why fail fast is really helpful. In combination with failing fast in this way, you can then use observability tools that are better inside into when errors are happening and why. The fourth reason why failing fast is good is that it helps you focus on real issues. Instead of trying to predict and handle every possible error, you focus on handling those errors, those exceptions that you know are likely and meaningful to your application. It also means that you will probably first deal with the errors that happen most often. It's very efficient way to increase the robustness of your code. In my case, that was even the direct reward associated with this. Because if there was an error in the invoice handling system, it meant that I had to generate the invoice manually. So every time I fixed an issue, it immediately saved me time. This gave me a really nice motivation to see the system handle something automatically that I needed to do manually before. For example, a common error that occurred is that we had invalid European VAT IDs. These are for taxes. There's a specific structure to European VAT ID. The first two characters should be the contract code and then there is a bunch of numbers. Many people forget to write the contract code and just write the number. That led to errors in our back and when it tried to generate the invoice. But since I could see a log of those errors and it failed fast, it didn't screw up my accounting system, they formed a guide that told me how I can make my code more robust. To avoid these VAT ID errors, I wrote a function to automatically add the contract code to VAT ID if it was missing. And this is a shorter version of what that function looks like. And as you can see, it also does some other clean-up work like removing spaces or an extra check like if the VAT ID has no numbers, then it's simply invalid. And these things I also added following an error that I saw in my back end. There are situations where you need to be extra careful around error handling. Sometimes it's hard to know that an error occurred. For example, if your software is embedded in a hardware device at the customer location, it's not connected to the internet. In that case, you need to do more upfront work and make sure that if the software crashes, the customer can restart the device without you having to come over physically. Also, don't just let things crash if that has severe consequences like when you're shooting a rocket into space. Or if you're working on software that part of a life-saving medical device. In this case, it's even more important to detect errors early on during the development phase so they don't cause a bigger problem later. When dealing with real-time systems, failing fast might also not be an option because this could disrupt ongoing operations. For example, let's say you work on software in the Netherlands that monitors the sea level and changes settings real-time in a water management system. Now, of course, you don't want the system to crash because of an error because well, then no one else. In such cases, you need to have robust error recovery strategies. Also, consider the user experience. If a failure would result in really poor user experience such as losing unsafe work, it's worth handling the error more gracefully. For example, you could catch the error at the top level, show methods to the user, store whatever they changed, and then report the error. It's also quite common in APIs that you need to handle errors at the top level. In that case, often an API returns a 500 internal server error. Now, it's better to actually report a useful error, but sometimes you've got to do what you've got to do. In terms of architecture, overusing the fail fast approach in a micro-services system might lead to a cascading failure where one service failing causes other servers to fail as well. One pattern that you can implement to avoid that is the circuit breaker pattern. This is like a safety switch for your services. If one server starts failing repeatedly, the circuit breaker trips and stops more requests from hitting the failing surface. This helps prevent one problem from causing a chain reaction of failures. And finally, failing fast doesn't mean you should just push crappy code and wait for it to crash. Make sure it's all right. You're a test to cover the edge cases. Coverage reports are a great way to determine what area of your code is not well tested. However, beware that the high coverage percentage can give you a false sense of security. Your test might evaluate your entire code base, but it's still possible that your code feels because of an error that you didn't anticipate. Before I finish the video, let's go over a few more quick tips to help you handle errors. If you like these tips, you might also like my free Discord server. I'd love for you to join the community. Go to discord.rndl.gov to join for free. The link is also in description. The first step is to avoid using bare accept clauses which catch all exceptions. I had an example of this in these Tribe API helpers where I try to put everything in a Tribe accept block that then just caught all exceptions. This is sort of what that looks like. The problem with this is that you may now catch and ignore errors that should be handled elsewhere or that at least you want to be informed of. For example, let's say that this function is being called from an API. And if we do the API call now, then this function is going to raise an error, but it will simply print it. And the piece of code that code this function is not going to know that an exception occurred. In Python, specifically, catching all exceptions like this might actually lead to you not catching syntax errors on name errors. So that's really something that you definitely want to avoid. The only place where these are sort of acceptable is really at the top level of your code as a mechanism to prevent a crash. For example, let's say you're running an API server and you want to make sure that your request always sends a response. So then at the top level, you just want to catch all exceptions. And then in the worst case, send a 500 internal server error response. Even though that is not preferred, but if it's the only possibility, then well, that's what you need to do, right? Second tip is to use context managers with statements to handle resource management. If an error occurs while you're reading a file, working with a database, the context manager will help you make sure that the file or the database connection is probably closed, which will help you avoid errors in the future. Here's a simple example of what that looks like. So let's say we have some code that loads a config file. Well, if there is some issue with opening this particular file, maybe it's not a text file, but some other format so you can't read it this way. Or perhaps it contains JSON data that's invalid. Well, then this is going to raise an error and the with statement is going to make sure that the file is properly closed. Third tip, if you're developing a package, use custom exception classes that fall under a hierarchy specific to your package. That way users or yourself can easily catch and handle all errors coming from your package. For example, here's the exception hierarchy of a package called Faker, which allows you to generate fake data. So this base exception that inherits from the exception class, and then all the other exceptions in this package, like a uniqueness exception or unsupported feature, are all subclasses of the base Faker exception. The nice thing about this is that you can then in your code, somewhere catch base Faker exceptions, and then if the makers of the packages did a good job, you're sure that any exception that you will catch will then specifically come from this library. The final tip is to make sure that you lock errors. If you don't know an error happened, well, you can fix it. Also ensure that error logging doesn't expose sensitive information. For example, don't log passwords, personal data, or sensitive configuration details or API keys. In summary, failing fast can simplify your code, help you catch bugs early on and make sure you are aware of serious errors. Now there are also some caveats that I mentioned, like don't just let things crash if there are serious consequences. You may have specific error handling needs if you dealing with real-time systems where you want to make sure that failing isn't an option. And I've also briefly mentioned that you need to be careful if you use fail fast in combination with microservices, and then you can implement something like the circuit breaker pattern. Now, I'd like to hear from you. Do you apply this fail fast print when you're code? Do we have any tips for handling errors? Let me know your thoughts in the comments. The code that I showed you today is based on a refactor that I did while a code where I started with a version that looked much worse and I covered a few key techniques that turned it into much cleaner code. To learn more, check out this video next. Thanks for watching and see you next time.